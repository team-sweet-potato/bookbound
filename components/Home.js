import React from "react";
import { ScrollView, Container, VStack, Text } from "native-base";

const Home = () => {
  return (
    <ScrollView>
      <VStack alignItems="center">
        <Container></Container>
      </VStack>
    </ScrollView>
  );
};
